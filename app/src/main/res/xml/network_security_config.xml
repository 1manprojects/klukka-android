<?xml version="1.0" encoding="utf-8"?>
<!--
    Allows cleartext HTTP traffic so the app can connect to local or HTTP-only
    servers. The login screen already warns the user when HTTP is used.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
