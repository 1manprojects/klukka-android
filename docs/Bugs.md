* User cannot start tracking project reliably. Especially Group Projects Tracking fails with java.lang.NumberFormatException: For input string: "true". The Backend starts tracking succesfully only app fails
* User cannot create new Project. It fails with Endpoint POST /api/user/addPersonalProject not found. Also the Color input should be a Color Selection and not a string input
* Wenn editing a Entry in the Calendar View. The calendar is not refreshed after succesfull save.
* Achived View shows Group Projects that are not archived.